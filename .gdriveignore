# ==========================================
# ไฟล์นี้บอก Google Drive ว่าอย่า sync อะไรบ้าง
# ==========================================

# โค้ดที่โหลดมาจากอินเตอร์เน็ต (ไม่ต้อง backup เพราะรัน npm install ได้ใหม่)
node_modules/

# ไฟล์ build ที่ generate ขึ้นมาเอง (ไม่ต้อง backup เพราะ build ใหม่ได้)
.next/
dist/
build/
out/

# ไฟล์ cache ชั่วคราว (ไม่มีประโยชน์เก็บไว้)
.cache/
*.log
.env.local

# ไฟล์ระบบ Windows (ไม่จำเป็น)
Thumbs.db
desktop.ini
